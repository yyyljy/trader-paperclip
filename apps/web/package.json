{
  "name": "@trader-paperclip/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm exec vite build",
    "dev": "pnpm exec vite",
    "preview": "pnpm exec vite preview",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "pnpm --dir ../.. exec vitest run --project web --config vitest.config.ts"
  },
  "dependencies": {
    "@trader-paperclip/config": "workspace:*",
    "@trader-paperclip/contracts": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
